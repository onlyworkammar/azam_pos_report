# Production Dockerfile for Cloud Build
# This expects the Flutter web app to be already built in ./build/web

FROM nginx:alpine

# Copy built files from Cloud Build step
COPY build/web /usr/share/nginx/html

# Copy nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
